import type { StyleObject } from 'styletron-react';

export const styles = {
  creatableOptionStyles: {
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    wordBreak: 'break-all',
  } as StyleObject,
  creatableIconStyles: {
    display: 'flex',
    alignItems: 'center',
  } as StyleObject,
};
