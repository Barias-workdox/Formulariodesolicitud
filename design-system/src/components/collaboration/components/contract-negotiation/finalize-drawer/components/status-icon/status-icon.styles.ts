import type { StyleObject } from 'styletron-react';

export const styles = {
  statusIconContainer: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  } as StyleObject,
};
