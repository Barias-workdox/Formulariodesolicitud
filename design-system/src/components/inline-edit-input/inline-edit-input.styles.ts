import { themedStyled } from '@themes/utilities';

export const StyledContainer = themedStyled('div', {
  display: 'flex',
  flexDirection: 'row',
  width: '100%',
  alignItems: 'center',
  height: '32px',
});
