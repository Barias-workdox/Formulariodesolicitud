import type { StyleObject } from 'styletron-standard';

/**
 * A style override for the content of the option.
 */
export const optionContentOverrideStyle: StyleObject = {
  wordBreak: 'break-word',
};
