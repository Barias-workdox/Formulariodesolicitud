import type { TextSelectionPosition } from './webdox-ai-document-viewer-wrapper.interfaces';

export const DEFAULT_TEXT_SELECTION_POSITION = {
  top: 100,
  left: 100,
} satisfies TextSelectionPosition;

export const TEXT_SELECTION_MARGIN = 30;
