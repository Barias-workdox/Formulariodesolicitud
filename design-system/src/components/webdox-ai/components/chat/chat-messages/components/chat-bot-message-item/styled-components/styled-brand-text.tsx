import { themedStyled } from '@themes/utilities';

/** A styled text with a special text used as the chat bot brand text */
export const StyledBrandText = themedStyled('span', () => ({
  fontWeight: 500,
}));
