import type { SharedProps } from 'baseui/input';

/**
 * Mocked shared props for Input component.
 */
export const sharedPropsMock: SharedProps = {
  $adjoined: 'none',
  $disabled: false,
  $isReadOnly: false,
  $size: 'default',
};
