import { DesignSystemTheme } from './theme.interfaces';
import { StyleObject } from 'styletron-react';
/**
 * Returns the focus-within style for a given theme.
 */
export declare const getFocusWithinStyles: (theme: DesignSystemTheme) => StyleObject;
