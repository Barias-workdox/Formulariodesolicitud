import { Items } from 'baseui/menu';
import { Options } from 'baseui/select';
/**
 * Get the index of an item in an array of items.
 */
export declare const getItemIndex: (items: Items | Options, id: string | number) => number | undefined;
