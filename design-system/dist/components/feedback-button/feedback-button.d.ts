import { FeedbackButtonProps } from './feedback-button.interfaces';
/**
 * Feedback button that returns a tooltip with the thumbs up or thumbs down interaction
 */
export declare const FeedbackButton: ({ "data-testid": dataTestId, feedbackKind, isActive, disabled, isLoading, showTooltip, type, tooltipProps, zIndex, onClick, }: FeedbackButtonProps) => JSX.Element;
