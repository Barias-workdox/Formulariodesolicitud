import { FileUploadManagerContextValue } from '../contexts/file-uploader-manager.context';
/**
 * Hook that returns the file uploader manager context value
 */
export declare const useFileUploadManagerContext: () => FileUploadManagerContextValue;
