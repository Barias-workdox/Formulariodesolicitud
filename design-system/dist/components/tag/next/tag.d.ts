import { TagProps } from './tag.interfaces';
/**
 * Tag component renders a tag with a background color and font color based on the variant and kind
 */
export declare const Tag: ({ "data-testid": dataTestId, kind, variant, shape, disabled, size, icon: Icon, showAction, actionIcon: ActionIcon, zIndex, children, $style, onClick, }: TagProps) => JSX.Element;
