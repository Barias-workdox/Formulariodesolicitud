import { SharedProps } from '../../../input.interfaces';
/**
 * Styled component for the prefix text
 * This component is used to display text before the input field.
 */
export declare const StyledPrefixText: import('styletron-react').StyletronComponent<"span", Pick<SharedProps, "$size" | "$disabled">>;
