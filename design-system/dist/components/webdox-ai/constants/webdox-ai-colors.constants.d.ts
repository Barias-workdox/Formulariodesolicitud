/**
 * These colors are intended for use exclusively within Webdox AI components
 * and are not standardized for the DS theme.
 */
export declare const WEBDOX_AI_COLORS: {
    primaryColor: string;
    secondaryColor: string;
    tertiaryColor: string;
};
