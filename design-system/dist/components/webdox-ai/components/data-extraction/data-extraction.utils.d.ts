import { DocumentMetadata } from '../../interfaces';
/**
 * Type guard to check if an item is a DocumentMetadata
 */
export declare const isDocumentMetadata: (item: unknown) => item is DocumentMetadata;
