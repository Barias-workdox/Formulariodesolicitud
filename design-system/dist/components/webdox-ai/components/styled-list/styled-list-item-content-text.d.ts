import { ReactElement } from 'react';
import { TruncatedTextProps } from '../../../truncated-text';
export type StyledListItemContentTextProps = TruncatedTextProps;
/** Text content used in the Styled List Item */
export declare const StyledListItemContentText: ({ children, tooltipProps, textProps, }: StyledListItemContentTextProps) => ReactElement;
