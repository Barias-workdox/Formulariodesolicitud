import { DesignSystemTheme } from '../../themes';
import { StyleObject } from 'styletron-standard';
/**
 * Generates the styles for the warning icon wrapper.
 */
export declare const warningIconWrapperStyles: (theme: DesignSystemTheme) => StyleObject;
/**
 * Generates the styles for the expired icon wrapper.
 */
export declare const expiredIconWrapperStyles: (theme: DesignSystemTheme) => StyleObject;
export declare const styles: {
    bodyWrapperStyles: (theme: DesignSystemTheme) => StyleObject;
    warningIconWrapperStyles: (theme: DesignSystemTheme) => StyleObject;
    expiredIconWrapperStyles: (theme: DesignSystemTheme) => StyleObject;
};
