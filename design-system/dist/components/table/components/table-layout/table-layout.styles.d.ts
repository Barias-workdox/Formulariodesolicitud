import { DesignSystemTheme } from '../../../../themes';
import { StyleObject } from 'styletron-react';
/**
 * Returns the style object for the table layout container.
 */
export declare const tableLayoutContainerStyles: (theme: DesignSystemTheme, gridTemplateColumns: string) => StyleObject;
/**
 * Returns the style object for the table separation line.
 */
export declare const tableSeparationLineStyles: (theme: DesignSystemTheme) => StyleObject;
