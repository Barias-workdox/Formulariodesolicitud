import { DesignSystemTheme } from '../../../../themes';
import { StyleObject } from 'styletron-react';
/**
 * Returns the style object for a table cell.
 */
export declare const tableCellStyles: (theme: DesignSystemTheme) => StyleObject;
