import { ReactElement } from 'react';
import { DynamicDialogHeaderProps } from '../dynamic-dialog.interfaces';
/**
 * Header component for the DynamicDialog
 */
export declare const DynamicDialogHeader: ({ dataTestId, title, icon, description, showDescription, showBackButton, actions, showActions, visible, className, onBackButtonClick, }: DynamicDialogHeaderProps) => ReactElement | null;
