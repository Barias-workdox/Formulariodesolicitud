import { ReactElement } from 'react';
import { DynamicDialogBodyProps } from '../dynamic-dialog.interfaces';
/**
 * Body component for the DynamicDialog
 */
export declare const DynamicDialogBody: ({ dataTestId, children, className, padding, }: DynamicDialogBodyProps) => ReactElement;
