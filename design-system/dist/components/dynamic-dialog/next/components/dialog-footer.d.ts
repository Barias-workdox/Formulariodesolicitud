import { ReactElement } from 'react';
import { DynamicDialogFooterProps } from '../dynamic-dialog.interfaces';
/**
 * Footer component for the DynamicDialog
 */
export declare const DynamicDialogFooter: ({ dataTestId, children, className, visible, }: DynamicDialogFooterProps) => ReactElement | null;
