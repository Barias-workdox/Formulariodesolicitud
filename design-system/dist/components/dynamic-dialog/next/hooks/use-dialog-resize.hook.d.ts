/**
 * Hook to determine if resize handles should be shown
 */
export declare const useDialogResize: (resizable: boolean, fullViewport: boolean, isMobile: boolean) => {
    shouldShowResizeHandles: boolean;
};
