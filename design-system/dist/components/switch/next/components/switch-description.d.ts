import { ReactElement } from 'react';
import { SwitchDescriptionProps } from '../switch.interface';
/**
 * SwitchDescription component that renders the description text for a switch component.
 * Handles proper styling, accessibility, disabled state, and ARIA described by functionality.
 */
export declare const SwitchDescription: ({ description, variant, disabled, ariaDescribedBy, }: SwitchDescriptionProps) => ReactElement | null;
