import { ReactElement } from 'react';
import { SwitchTitleProps } from '../switch.interface';
/**
 * SwitchTitle component that renders the title text for a switch component.
 * Handles proper styling, accessibility, and disabled state.
 */
export declare const SwitchTitle: ({ title, variant, disabled, }: SwitchTitleProps) => ReactElement | null;
