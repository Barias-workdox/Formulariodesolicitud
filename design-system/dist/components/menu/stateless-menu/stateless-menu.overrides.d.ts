import { StatelessMenuProps } from './stateless-menu';
import { MenuOverrides } from 'baseui/menu';
/**
 * Gets the base overrides for the StatelessMenu component with options to customize it.
 */
export declare const getBaseOverrides: ({ dataTestId, items, itemLabelKey, itemLabelTemplate, optionListBorderBottom, placementChildMenu, menuWidth, }?: Partial<StatelessMenuProps>) => MenuOverrides;
