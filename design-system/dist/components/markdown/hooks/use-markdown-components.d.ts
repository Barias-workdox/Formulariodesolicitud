import { MarkdownComponents } from '../markdown.interfaces';
/**
 * Map of markdown elements to React components, enabling customized styling and
 * behavior for each element.
 */
export declare const useMarkdownComponents: () => MarkdownComponents;
