import { ListItemProps } from './list-item.interfaces';
/**
 * Component that represents an item for a list.
 * It supports different interactive states and allows customization through
 * overrides and special properties.
 */
export declare const ListItem: import('react').ForwardRefExoticComponent<ListItemProps & import('react').RefAttributes<HTMLButtonElement | HTMLLIElement>>;
