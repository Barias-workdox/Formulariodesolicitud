import { ReactElement } from 'react';
export interface ListItemCounterProps {
    quantity: number;
}
/**
 * Renders a counter for the list item.
 */
export declare const ListItemCounter: ({ quantity }: ListItemCounterProps) => ReactElement;
