import { DataTableContextValues } from '../data-table.interfaces';
/**
 * Custom hook for accessing the Data Table context.
 *
 * This hook allows components within the Data Table to access the context's values
 * and functionality.
 */
export declare const useDataTableContext: () => DataTableContextValues;
