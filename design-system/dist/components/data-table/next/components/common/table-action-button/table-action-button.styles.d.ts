import { DesignSystemTheme } from '../../../../../../themes';
import { StyleObject } from 'styletron-react';
export declare const styles: {
    actionButtonStyles: (theme: DesignSystemTheme, { isDisabled }: {
        isDisabled?: boolean | undefined;
    }) => StyleObject;
};
