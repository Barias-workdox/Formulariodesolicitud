import { ReactElement } from 'react';
type BooleanTextProps = {
    value: boolean;
};
/**
 * Renders a localized text representation of a boolean value.
 */
export declare const BooleanText: ({ value }: BooleanTextProps) => ReactElement;
export {};
