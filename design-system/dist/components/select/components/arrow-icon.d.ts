import { ReactElement } from 'react';
import { ArrowIconProps } from '../select.interfaces';
/**
 * The ArrowIcon will be used to show if the select is showing its content or not
 */
export declare const ArrowIcon: ({ isOpen, isBorderless, color, }: ArrowIconProps) => ReactElement;
