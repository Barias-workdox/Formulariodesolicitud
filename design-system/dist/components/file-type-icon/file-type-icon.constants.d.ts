import { FileType, FileTypeIconConfig } from './file-type-icon.interfaces';
export declare const DEFAULT_ICON_SIZE = 24;
/**
 * Configuration object mapping file extensions to their respective icon styles.
 *
 * @remarks
 * The colors used here are not encapsulated within the theme and are used specifically for this scenario.
 */
export declare const FILE_ICON_CONFIG: Record<FileType, FileTypeIconConfig>;
