import { ReactElement } from 'react';
import { SidebarHeaderProps } from '../../sidebar.interface';
/**
 * SidebarHeader: Component for the header section of the Sidebar.
 * Remains fixed at the top with proper keyboard navigation order.
 */
export declare const SidebarHeader: ({ children, showTrigger, }: SidebarHeaderProps) => ReactElement;
