import { Size } from '../../input/next';
import { DesignSystemTheme } from '../../../themes/theme.interfaces';
import { StyleObject } from 'styletron-react';
/** Returns the icon size for the select ui based on the input size */
export declare const getTextStyles: (size: Size, theme: DesignSystemTheme) => StyleObject;
