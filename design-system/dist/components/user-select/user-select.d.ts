import { UserSelectProps } from './user-select.interface';
/** Component extending SelectWithPagination with custom label options to list users */
export declare const UserSelect: ({ "data-testid": dataTestId, disabled, creatable, avatarBackGroundColor, isLoadingMore, onLoadMore, onChange, placeholder, kind, overrides, size, options, ...rest }: UserSelectProps) => JSX.Element;
