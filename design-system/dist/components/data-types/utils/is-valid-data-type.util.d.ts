import { DataType } from '../interfaces/data-types.interfaces';
/**
 * Utility function to check if a given id is a valid DataType.
 */
export declare const isValidDataType: (id: string) => id is DataType;
