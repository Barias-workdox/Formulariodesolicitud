import { ProgressBarProps } from '.';
/** gets the overrides for the progress bar */
export declare const getProgressBarOverrides: ({ completed, }: {
    completed: boolean;
}) => ProgressBarProps["overrides"];
