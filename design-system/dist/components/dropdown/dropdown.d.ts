import { PropsWithChildren, ReactElement } from 'react';
import { DropdownProps } from './dropdown.interfaces';
/**
 * Dropdown component to show a menu with a list of items
 */
export declare const Dropdown: ({ dataTestId, options, children, size, textForTooltip, placement, buttonKind, }: PropsWithChildren<DropdownProps>) => ReactElement;
