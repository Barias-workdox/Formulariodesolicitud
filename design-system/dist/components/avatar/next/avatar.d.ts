import { AvatarProps } from './avatar.interface';
/**
 * The `Avatar` component displays a user's avatar with customizable properties such as
 * background color, size, kind, appearance, and optional disabled state.
 */
export declare const Avatar: ({ dataTestId, disabled, size, initials, name, overrides, zIndex, src, kind, appearance, clickable, onClick, href, showTooltip, }: AvatarProps) => JSX.Element;
