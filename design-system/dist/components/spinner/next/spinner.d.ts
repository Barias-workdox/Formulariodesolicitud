import { ReactElement } from 'react';
import { SpinnerProps } from './spinner.interface';
/**
 * Unified Spinner component that replaces both Spinner and FullSpinner
 */
export declare function Spinner({ dataTestId, kind, size, label, ariaLabel, fullWidth, isRelative, opacity, backgroundColor, delay, customColor, customSize, }: SpinnerProps): ReactElement;
