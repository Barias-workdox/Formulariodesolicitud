import { StyledIconProps, StyledRootProps } from './background-icon.interfaces';
/**
 * Styled root container for the BackgroundIcon component.
 * Handles size, shape, background color, and disabled states.
 */
export declare const StyledRoot: import('styletron-react').StyletronComponent<"div", StyledRootProps>;
/**
 * Styled wrapper for the icon element.
 * Handles icon color and disabled states.
 */
export declare const StyledIconWrapper: import('styletron-react').StyletronComponent<"span", StyledIconProps>;
