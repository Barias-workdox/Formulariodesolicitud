import { BackgroundIconProps } from './background-icon.interfaces';
/**
 * Component that renders a background icon with customizable properties.
 */
export declare const BackgroundIcon: ({ "data-testid": dataTestId, backgroundColor, children, disabled, Icon, iconColor, onClick, overrides, shape, size, ...rest }: BackgroundIconProps) => JSX.Element;
