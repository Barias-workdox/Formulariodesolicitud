import { Overrides } from '../../../../../../../../../themes/theme.interfaces';
/**
 * Returns the style overrides for the IconButton component
 */
export declare const iconButtonOverrides: () => Overrides;
