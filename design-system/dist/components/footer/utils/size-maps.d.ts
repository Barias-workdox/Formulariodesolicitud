import { FooterSize } from '../footer.interfaces';
import { SpacingKey } from '../../../themes/v3/tokens';
/**
 * Utility to get gap size from footer size.
 */
export declare const getGapSize: (size?: FooterSize) => SpacingKey;
/**
 * Utility to get padding size from footer size.
 */
export declare const getPaddingSize: (size?: FooterSize) => SpacingKey;
