import { FilterProps } from '../../../filter/filter.interfaces';
/**
 * Component that extends `Filter` to apply specific logic for `FiltersGroup`.
 */
export declare const CustomFilter: ({ overrides, ...rest }: FilterProps) => JSX.Element;
