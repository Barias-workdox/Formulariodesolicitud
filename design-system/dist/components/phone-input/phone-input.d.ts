import { PhoneInputProps } from './phone-input.interfaces';
/**
 * Component used to write phone numbers,
 * contains a country selector which automatically writes the dial code
 * of the selected country and a tel input to write the phone number.
 */
export declare const PhoneInput: ({ "data-testid": dataTestId, overrides, maxDropdownHeight, maxDropdownWidth, ...rest }: PhoneInputProps) => JSX.Element;
